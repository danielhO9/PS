#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
	string s; cin >> s;
	for (auto i: s) if (i != '.') cout << i;
}

int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	solve();
}