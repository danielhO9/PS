#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
	int n, c, k; cin >> n >> c >> k;
	string s; cin >> s;
	int presum[c][n + 1]; memset(presum, 0, sizeof(presum));
	for (int i = 0; i < c; ++i) {
		for (int j = 0; j < )
	}
}

int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	int t; cin >> t;
	while (t--)solve();
}