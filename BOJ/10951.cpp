#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int A, B;
    while (true) {
        cin >> A >> B;
        if(cin.eof()) break;
        cout << A + B << "\n";
    }
    return 0;
}