#include <bits/stdc++.h>
using namespace std;

void solve() {
	int N, M; cin >> N >> M;
	while (M--) {
		int a, b; cin >> a >> b;
	}
	cout << N - 1 << '\n';
}

int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	int t; cin >> t;
	while (t--) solve();
}